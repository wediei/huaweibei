# O4.1 剩余来源闭环卡

## 当前结论

`PARTIAL`：O4.1 已在全新路径精确复现，新 NPY 与保留 NPY 的 SHA-256 均为 `f474dcc61ee5418954a28437bdb09aefdafcfa266559133e130db04c404bfe8c`；但未找到平台回执，不能把该产物绑定到线上 `0.552317`。

## 已闭合

- 12 个 O4.1 相关服务器源码与起始提交 Git blob 匹配。
- day10、day13、day18 checkpoint 与哈希边车匹配。
- day3/day7/day10/day13/day14/day18 资产已有逐文件清单。
- 新 NPY 格式有效并与保留 NPY 字节级一致。
- 保留 ZIP 仅包含该 NPY，成员哈希一致。

## 尚缺证据

需要可信平台回执，至少包含提交时间、提交标识或文件名及分数 `0.552317`；最好包含能与保留 ZIP 对应的上传 ID 或哈希。

## 后续边界

不需要重新训练或再次推理。若回执无法恢复，应把 `0.552317` 保持为未哈希绑定的历史分数，不得标记为“已闭合可复现基线”。
