# 020-ac-cgmrf 执行报告

## 任务定义

- 目标：在冻结 O4.1 旁边实现可生成 Anchor 外新 Beam-Delay 复支撑的 AC-CGMRF，并按严格空间和因果门槛决定是否晋级。
- 起始提交：`2ba9796d4eefe7e4c08e5eb4366cba274a18a226`
- 批准的 Spec：`specs/020-ac-cgmrf.md`
- 工作范围：模型、训练、评估、空间折、泄漏防护、real/zero/shuffle、support-novelty、日志、checkpoint 哈希和条件式推理。
- 明确不做：不改 O4.1/Power Refiner/K=4 Anchor/缓存契约，不安装依赖，不用新 CUDA 扩展，不自动上传榜单。

## Git 身份

- GitHub：`https://github.com/wediei/huaweibei`
- 分支：`task/020-ac-cgmrf`
- 起始提交：`2ba9796d4eefe7e4c08e5eb4366cba274a18a226`
- 最终提交 SHA：本报告收口提交，以远程分支 HEAD 和交付回执中的精确 SHA 为准。

## 完成内容

- 新增小容量共享地图/Anchor 条件编码器，显式使用 Target/Anchor/Anchor→Target Gaussian token、位置、Anchor 复信道摘要、O4.1 复幅相和 reliability。
- 新增 16–32 可配置连续复高斯原子；默认 24 个。中心、宽度、相位、存在性、可靠度均受限，极化耦合为复数低秩外积。
- 新增纯 PyTorch 局部 Beam-Delay splat；Beam 轴循环、Delay 轴截断，所有原子在复数域相干求和，不构造 Atom×全网格稠密张量。
- 新增 Anchor/O4.1 相位参考、连续 Delay DFT phase-ramp、残差能量上限和零初始化 signed trust gate。
- 新增 real/zero/shuffle、support-novelty、复信道/PAS/PDP/官方 score/能量/稀疏/因果/极化/相位连续性联合损失。
- 新增确定性空间块 K 折、验证 Target 自排除、验证集不得作为 Anchor、单折淘汰和完整交叉验证门槛。
- 新增 `make-folds`、`prepare-fold-cache`、`train`、`evaluate`、`aggregate-cv`、`infer`。只有通过完整空间验证且使用 full checkpoint，`infer` 才放行。
- 训练入口生成 batch/epoch tqdm、`metrics.jsonl`、`config.json`、`best.pt`、`last.pt`、SHA-256 边车和 `train_report.json`。

## 验证或运行结果

- 新增定向测试：15 项通过。
- 全量回归：174 项通过。
- `python -B -m compileall`：通过。
- `git diff --check`：通过。
- 恒等回退：单元测试为精确相等，目标 `max_abs_error <= 1e-6` 已覆盖。
- 复数 splat：同相加倍、反相抵消、位置与复增益梯度、DFT phase-ramp、确定性均通过。
- SSH 真实资产预检：通过。O4.1 恒等回退 `max_abs_error=0.0`；输出为 `torch.complex64`、shape `[2, 256, 4, 192]` 且 finite；复数 splat 梯度非零且 finite；K=4 和验证 Target Anchor 排除均通过。
- SSH 泄漏审计：通过。训练/验证数为 1800/200，检查 800 个验证 Anchor，`target_self_leaks=[]`、`validation_anchor_leaks=[]`。
- SSH 单折先导：完成 9 个 epoch 后 early stopping；最佳 checkpoint SHA-256 为 `434b0db2760b1d324357a58e2ae265c7c8bb7644e6bbdd66f8f677f153d217fc`。
- 最佳 checkpoint 独立评估：O4.1 基线和 real 最优分数均为 `0.6901056736118361`，real 最优比例为 `0.0`，单折增益为 `0.0`。
- 因果与新支撑诊断：real-over-shuffle 为 `0.0`，support novelty 为 `0.0`；real 残差能量比仅 `3.1342551755353385e-10`，模型退化为恒等回退。
- 指标保护：在 `scale=0` 下 NMSE/PAS/PDP 与 O4.1 完全相同，因而没有恶化，但也没有任何提升。
- Pilot gate 明确返回 `decision=STOP`：`gain_at_least_0_01=false`、`real_over_shuffle_at_least_0_005=false`、`support_novelty_nontrivial=false`。
- 完整空间交叉验证、全量训练和官方测试 NPY：按 Spec 硬停止条件未运行。

## 未完成与异常

- 单折增益 `0.0 < +0.01`，触发研究淘汰门槛。
- real-over-shuffle `0.0 < +0.005`，再次触发研究淘汰门槛。
- support novelty 为 `0.0`，零初始化 trust gate 最终仅产生近零残差，未形成有效的新 Beam-Delay 支撑。
- 依照 Spec，未继续完整空间交叉验证、全量训练或官方测试推理，也未生成官方测试 NPY。
- 未安装或重建任何环境，也未引入新依赖。

## 外部产物

- SSH 预检报告：`/root/autodl-tmp/solution/artifacts/day20/ac_cgmrf_preflight_seed42.json`
- SSH 单折目录：`/root/autodl-tmp/solution/artifacts/task020/ac_cgmrf_pilot_seed42/`
- 产物：`metrics.jsonl`、`config.json`、`best.pt`、`best.pt.sha256`、`last.pt`、`last.pt.sha256`、`train_report.json`、`evaluate_report.json`
- `best.pt` SHA-256：`434b0db2760b1d324357a58e2ae265c7c8bb7644e6bbdd66f8f677f153d217fc`
- `last.pt` SHA-256：`cf8b17bde854c61f88f671b0c39b4533433f1a0f3201cecffec11a04785b0db2`
- 冻结 O4.1 checkpoint SHA-256：`7d0fe4c3e8fd91511069b4f57dce317d573a230aefb88d33787791b95cf8f783`
- 大型 checkpoint、缓存和日志保留在 Git 外部；没有生成 NPY。

## 给架构师的结论

- 一句话结果：AC-CGMRF 工程、恒等回退和泄漏防护均通过，但 SSH 单折最佳 checkpoint 对 O4.1 的增益、real-over-shuffle 和 support novelty 全为 0，触发硬停止。
- 对总体方案的影响：本实现证明该零 trust gate 训练配置会塌缩到 O4.1 恒等回退，不能支撑完整空间验证或官方测试推理；任何进一步修正都属于新的研究迭代。
- 建议：`DISCARD`。
