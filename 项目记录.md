# 项目记录

## 2026-07-30：长期项目上下文 Skill 改造

- 用户要求：首次接管原始项目只读说明文件；获批进入创新探索后，架构师应主动读取必要的当前代码和 reference 核心实现，不再由用户指定文件。
- 修改结果：`manage-project-context` 改为按架构师、执行者、验证者和交接阶段渐进加载；重要候选版本由短期只读验证子代理检查。
- 验证结果：Skill 静态校验通过，三个隔离角色场景通过；首次架构师必需材料由约 31.1 KB 降至约 9.6 KB，减少约 69%。
- 评价：能明显减少 Skill 自身造成的启动上下文，但 Codex 固定系统说明、工具定义和项目级指令仍会占用上下文。

## 2026-07-30：首次接管继续压缩

- 用户反馈：真实首次接管仍占约 30% 上下文，临时理解和记录草案重复，并提前制定研究门槛；同时要求架构师能主动从源码寻找灵感，但不能机械通读全仓。
- 修改结果：架构师协议进一步拆为首次接管、持续架构和创新研究三阶段；明确迭代记录不存在时初始化、存在时接续维护。
- 源码策略：按概览、接口、数据流、核心机制逐层深入，每次加深必须对应具体信息缺口，证据充分后立即停止。
- 验证结果：三个独立行为场景通过；首次接管必需 Skill 材料降至约 7.2 KB。

## 2026-07-30：基线措辞边界

- 用户反馈：架构师把“当前线上最佳且可回退”误写成“不可破坏”，可能限制后续创新。
- 修改结果：Skill 明确区分项目事实与治理决策；没有用户或正式记录授权时，不得把当前最佳、保留、接受或可回退推导为冻结、不可替换。
- 验证结果：五个独立措辞样本均正确保留事实，没有产生冻结含义。

## 2026-07-30：源码探索上下文隔离

- 用户反馈：一次创新研究在架构师对话中直接展开约 2921 行源码和 8 页任务书，导致上下文达到约 70%。
- 修改结果：单轮研究限制为一个瓶颈和一个 reference；架构师直接读取最多六个片段、每段约 100 行、合计约 600 行。
- 子代理策略：超过预算或需要多模块追踪时，交给短期源码探索子代理；原始源码留在子代理上下文，只返回紧凑结论、证据索引和最多两个决定性片段。
- 验证结果：少量直接读取、超预算自动委派和紧凑回传三个行为场景均通过。

## 2026-07-30：019 O4.1 来源闭环

- 用户要求：只读盘点服务器 O4.1 资产，核对 commit、checkpoint、NPY、ZIP 和线上分数；前置条件满足时才在新路径推理。
- 修改结果：关键资产和三份 checkpoint 边车有效；12 个相关源码匹配起始提交 Git blob；新 NPY 与保留 NPY 哈希一致。
- 验证结果：shape、dtype、finite 和 SHA-256 全部通过；未训练、未覆盖既有资产、未上传榜单。
- 评价：计算复现链已闭合，但缺平台回执，按 Spec 判定 PARTIAL，建议 REVISE。

## 2026-07-30：020 AC-CGMRF 工程实现

- 用户要求：冻结 O4.1，在旁路新增可生成新 Beam-Delay 复多径支撑的 AC-CGMRF，并以严格空间折和 real/zero/shuffle 门槛验证。
- 修改结果：新增共享条件编码、24 个低秩极化复高斯原子、纯 PyTorch 局部相干 splat、零 trust gate、联合损失、空间泄漏审计和门槛保护的训练/评估/推理 CLI。
- 验证结果：恒等回退、shape/dtype、复数叠加、梯度、确定性、support-novelty、空间折和 checkpoint 身份测试通过；原有 O4.1 回归未受影响。
- 评价：工程阶段完成；单折先导和完整空间交叉验证须由用户在 SSH 运行，未过门槛前禁止官方测试推理。
