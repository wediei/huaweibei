# 华为杯 Round1 项目总结

## 1. 清理后的项目边界

本项目只保留以下内容：

- `task/`：赛事任务书，原样保留。
- `reference/`：参考项目源码，原样保留。
- `dataset/`：赛事官方数据，原样保留。
- `test/`：用户原有的 WRFGS+ 改造基线，原样保留。
- `solution/`：仅保留线上得分 `0.552317` 的 O4.1 方案及其完整运行依赖。
- `results/8_0.552317/`：该得分对应的官方测试结果 `.npy` 与上传 `.zip`。
- `总结.md`：唯一继续维护的项目过程文档。

清理删除的是 Codex 后续产生但未超过 `0.552317` 的实验代码、测试、日志、缓存、报告、
旧提交结果、服务器执行文档和过程管理文件。

## 2. 当前唯一有效方案：O4.1

### 2.1 最终成绩

- 官方线上得分：`0.552317`
- 前一可部署基线 Power Refiner V1：`0.547709`
- 线上增益：`+0.004608`
- 本地验证 coarse：`0.683729`
- O4.1 本地验证：`0.690106`
- 本地增益：`+0.006376`
- 最佳修正尺度：`0.5`
- 本地 PAS：`0.6967405566`
- 本地 PDP：`0.7893297821`
- 本地 NMSE：`1.0903547795`
- 真实地图相对最佳 zero/shuffle 控制组优势：`+0.006376`

本地增益向线上的转化率约为 `72.3%`。这是整个项目中唯一经过官方线上验证、且明确超过
Power Refiner 基线的地图增强方案。

### 2.2 方案结构

O4.1 不是从坐标直接生成完整 CSI，也不是传统图像 3DGS 渲染。它是一个以实测邻近信道为
基础、由官方点云 Gaussian 传播路径引导的低维信道运输方案：

1. **固定 Beam-Delay 压缩**
   - 将完整复信道变换为固定支持的 Beam-Delay latent。
   - 避免在 2000 个训练样本上使用大解码器从零预测约 20 万个复数值。

2. **冻结 A2 Direct-score coarse**
   - 使用邻近训练 Anchor 的真实信道产生粗信道。
   - A2 checkpoint 启用了 `geometry_1m.npz` 地图几何输入。

3. **冻结 Power Refiner V1**
   - 对 coarse 做低维 group 功率/幅相修正。
   - 官方测试使用全部 2000 个训练 Anchor。
   - 固定 `power_scale=1.25`。

4. **官方 PLY Gaussian 场景**
   - 将 `Round1_Map.ply` 体素聚合为固定各向异性 Gaussian：
     center、normal、切向/法向尺度、opacity。
   - 每条传播走廊选取 48 个 Gaussian token。

5. **逐 Anchor 信道运输**
   - 对 K=4 个邻近 Anchor 分别保留完整 Beam-Delay 信道，不先做聚合。
   - 每个 Anchor 独立结合以下地图上下文预测低维运输参数：
     - `BS→Target` Gaussian 路径；
     - `BS→Anchor` Gaussian 路径；
     - 两条路径的差；
     - O4.1 新增的真实 `Anchor→Target` Gaussian 直达走廊。
   - 输入由 O4 的 81 维扩展为 O4.1 的 108 维。

6. **低维连续运输与融合**
   - 只预测每个 P/N group 的 H/V/Delay shift、复增益、存在性和可靠度。
   - 使用 DFT phase-ramp 完成连续 Beam-Delay 运输。
   - K=4 Anchor 使用固定距离权重聚合。
   - 最终输出为：
     `coarse + 0.5 × (transported - coarse)`。

7. **地图因果约束**
   - zero-map 分支必须精确回退 coarse。
   - shuffle-map 将地图路径与位置错配。
   - O4.1 的 real-map 明确高于 zero/shuffle，说明提升来自正确地图—信道绑定，而不只是
     通用神经网络校正。

### 2.3 O4.1 的关键代码

核心入口：

- `solution/radio_map/learning/gaussian_anchor_path_cli.py`
- `solution/radio_map/learning/gaussian_anchor_transport_cli.py`

核心实现：

- `gaussian_anchor_path_cache.py`
- `gaussian_anchor_transport.py`
- `gaussian_path_transport.py`
- `gaussian_path_transport_cli.py`
- `gaussian_token_cache.py`
- `gaussian_token_cli.py`
- `gaussian_geometry.py`
- `path_transport.py`
- `coarse_provider.py`
- `power_refiner_cli.py`

其余保留的 `solution/radio_map` 文件是上述入口的实际传递依赖，不应随意删除。

## 3. 服务器资产

本地目录保留源码和得分文件，但没有保存服务器训练产生的全部 checkpoint/缓存。要复现或推理，
服务器必须具备：

```text
dataset/Round1_Map/
solution/artifacts/day3/geometry_1m.npz
solution/artifacts/day7/cache_seed42_stable/
solution/artifacts/day10/a2_directscore_seed42/best.pt
solution/artifacts/day10/a2_directscore_seed42/best.pt.sha256
solution/artifacts/day13/power_refiner_seed42/best.pt
solution/artifacts/day13/power_refiner_seed42/best.pt.sha256
solution/artifacts/day14/gaussian_scene_1m.npz
solution/artifacts/day14/gaussian_tokens_seed42/
solution/artifacts/day14/o3_supervision_seed42/
solution/artifacts/day18/anchor_target_paths_k4/
solution/artifacts/day18/o4_direct_path_seed42/best.pt
solution/artifacts/day18/o4_direct_path_seed42/best.pt.sha256
```

如果这些服务器资产已经删除，仅靠本地源码无法还原同一个 `0.552317` checkpoint，必须按下面的
顺序重新构建和训练。

## 4. 运行环境

SSH 服务器使用 mamba，不使用 `conda run`：

```bash
cd /root/autodl-tmp
source /root/miniconda3/etc/profile.d/conda.sh
mamba env create -f solution/environment-train.yml
mamba activate huaweibei-train
```

PyTorch 故意不写入环境 YAML，单独安装 CUDA 11.8 wheel：

```bash
pip install torch==2.0.1 torchvision==0.15.2 torchaudio==2.0.2 \
  --index-url https://download.pytorch.org/whl/cu118
```

## 5. O4.1 复现流程

### 5.1 准备 Gaussian scene 与 BS→Target token

```bash
mkdir -p solution/artifacts/day14

python -m solution.radio_map.learning.gaussian_token_cli prepare \
  --data-dir dataset/Round1_Map \
  --cache-dir solution/artifacts/day7/cache_seed42_stable \
  --scene-cache solution/artifacts/day14/gaussian_scene_1m.npz \
  --output-dir solution/artifacts/day14/gaussian_tokens_seed42 \
  --voxel-size 1.0 \
  --tokens-per-path 48 \
  --candidate-k 12 \
  --path-samples 16 \
  --elevated-quota 16
```

### 5.2 准备冻结 coarse 与运输监督

```bash
python -m solution.radio_map.learning.gaussian_path_transport_cli prepare \
  --data-dir dataset/Round1_Map \
  --cache-dir solution/artifacts/day7/cache_seed42_stable \
  --base-checkpoint solution/artifacts/day10/a2_directscore_seed42/best.pt \
  --refiner-checkpoint solution/artifacts/day13/power_refiner_seed42/best.pt \
  --geometry-cache solution/artifacts/day3/geometry_1m.npz \
  --power-scale 1.25 \
  --anchor-source all_official_train \
  --device cuda \
  --batch-size 8 \
  --label-batch-size 4 \
  --output-dir solution/artifacts/day14/o3_supervision_seed42
```

### 5.3 构建 Anchor→Target 直达走廊

```bash
mkdir -p solution/artifacts/day18

python -m solution.radio_map.learning.gaussian_anchor_path_cli prepare \
  --data-dir dataset/Round1_Map \
  --cache-dir solution/artifacts/day7/cache_seed42_stable \
  --scene-cache solution/artifacts/day14/gaussian_scene_1m.npz \
  --output-dir solution/artifacts/day18/anchor_target_paths_k4 \
  --anchor-count 4 \
  --anchor-source all_official_train \
  --tokens-per-path 48 \
  --candidate-k 12 \
  --path-samples 16 \
  --elevated-quota 16
```

### 5.4 训练 O4.1

```bash
python -m solution.radio_map.learning.gaussian_anchor_transport_cli train \
  --data-dir dataset/Round1_Map \
  --cache-dir solution/artifacts/day7/cache_seed42_stable \
  --base-checkpoint solution/artifacts/day10/a2_directscore_seed42/best.pt \
  --refiner-checkpoint solution/artifacts/day13/power_refiner_seed42/best.pt \
  --geometry-cache solution/artifacts/day3/geometry_1m.npz \
  --power-scale 1.25 \
  --anchor-source all_official_train \
  --supervision-cache solution/artifacts/day14/o3_supervision_seed42 \
  --gaussian-token-cache solution/artifacts/day14/gaussian_tokens_seed42 \
  --anchor-path-cache solution/artifacts/day18/anchor_target_paths_k4 \
  --run-dir solution/artifacts/day18/o4_direct_path_seed42 \
  --anchor-count 4 \
  --device cuda \
  --seed 42 \
  --epochs 30 \
  --batch-size 4 \
  --learning-rate 5e-5 \
  --weight-decay 1e-4 \
  --training-scale 0.75 \
  --trust-weight 0.001 \
  --causal-margin 0.003 \
  --causal-margin-weight 1.0 \
  --patience 8
```

训练使用 batch 级 tqdm，并生成 `metrics.jsonl`、`best.pt`、`last.pt`、SHA-256 边车和
`train_report.json`。原结果在第 27 轮早停。

### 5.5 验证

```bash
python -m solution.radio_map.learning.gaussian_anchor_transport_cli evaluate \
  --data-dir dataset/Round1_Map \
  --cache-dir solution/artifacts/day7/cache_seed42_stable \
  --base-checkpoint solution/artifacts/day10/a2_directscore_seed42/best.pt \
  --refiner-checkpoint solution/artifacts/day13/power_refiner_seed42/best.pt \
  --geometry-cache solution/artifacts/day3/geometry_1m.npz \
  --power-scale 1.25 \
  --anchor-source all_official_train \
  --supervision-cache solution/artifacts/day14/o3_supervision_seed42 \
  --gaussian-token-cache solution/artifacts/day14/gaussian_tokens_seed42 \
  --anchor-path-cache solution/artifacts/day18/anchor_target_paths_k4 \
  --checkpoint solution/artifacts/day18/o4_direct_path_seed42/best.pt \
  --anchor-count 4 \
  --device cuda \
  --batch-size 4 \
  --output solution/artifacts/day18/o4_direct_path_seed42/evaluate_report.json
```

### 5.6 生成官方测试文件

```bash
mkdir -p solution/submissions/o4_direct_path_seed42

python -m solution.radio_map.learning.gaussian_anchor_transport_cli infer \
  --data-dir dataset/Round1_Map \
  --cache-dir solution/artifacts/day7/cache_seed42_stable \
  --base-checkpoint solution/artifacts/day10/a2_directscore_seed42/best.pt \
  --refiner-checkpoint solution/artifacts/day13/power_refiner_seed42/best.pt \
  --geometry-cache solution/artifacts/day3/geometry_1m.npz \
  --power-scale 1.25 \
  --anchor-source all_official_train \
  --supervision-cache solution/artifacts/day14/o3_supervision_seed42 \
  --gaussian-token-cache solution/artifacts/day14/gaussian_tokens_seed42 \
  --anchor-path-cache solution/artifacts/day18/anchor_target_paths_k4 \
  --checkpoint solution/artifacts/day18/o4_direct_path_seed42/best.pt \
  --anchor-count 4 \
  --device cuda \
  --batch-size 4 \
  --output solution/submissions/o4_direct_path_seed42/Round1_Test_Channel.npy

python -m solution.radio_map.cli validate-submission \
  --data-dir dataset/Round1_Map \
  --submission solution/submissions/o4_direct_path_seed42/Round1_Test_Channel.npy \
  --output solution/submissions/o4_direct_path_seed42/validation.json
```

提交文件必须为：

```text
shape = [500, 256, 4, 192]
dtype = complex64
finite = true
```

## 6. 失败尝试总表

以下内容均已从活动代码中删除，只保留结论，避免新对话重复投入。

### 6.1 原始与早期基线

| 尝试 | 结果 | 失败原因 |
|---|---:|---|
| 用户早期 WRFGS+ 改造 | 约 `0.49` | 15000 Gaussian 最终被大 ChannelDecoder 解码；地图被压缩成极少特征，过拟合严重 |
| A1 邻近信道压缩 | `0.503536` | 无地图；少样本下只能依赖邻近信道 |
| A2 地图几何增强 | `0.504074` | 地图输入过于粗糙，提升主要来自压缩抑制过拟合 |
| 固定 Gaussian Geometry | `0.509004` | 只做路径统计特征，不是真正的多径运输 |
| Direct-score coarse | `0.539649` | 明显提升，但仍缺少有效地图传播结构 |
| Phase fine-tune / KNN / 全 2000 Anchor | `0.540620`、`0.542422` | 只做局部相位搬运，提升有限 |
| Power Refiner V1 | `0.547709` | 有效的 O4.1 前置基线，已保留；单独使用仍未达到目标 |

### 6.2 Gaussian 运输 O3–O8

| 尝试 | 结果 | 失败原因 |
|---|---:|---|
| O3 参数监督 | 验证总是 `scale=0` | 参数标签 loss 下降，但不对应赛事得分 |
| O3 Direct-score | real `0.688069`，低于 zero/shuffle | 提升来自通用校正，不来自正确地图 |
| O3 Causal/Anchor-delta | real 仍不优于 shuffle | 地图—来源 Anchor 语义不足 |
| O3 Multi-anchor | real-over-shuffle `+0.000609` | 方向正确但远低于 `+0.003` 门槛 |
| O4（不含直达走廊） | 本地 `0.688353` | 有因果增益，但低于 O4.1；未提交 |
| O5 group-wise Anchor 竞争 | 最高 `0.684259` | 验证大多选择 `scale=0`，低于 O4.1 |
| O6 独立 Gaussian Radio Field | `0.688155`，margin `+0.000778` | 约 3050 万无线参数被约 1800 个位置严重欠约束 |
| O7 多尺度共享 Radio Field | 最佳 `0.687798` | 减少参数后仍没有突破运输参数预测瓶颈 |
| O8 插值运输场 | 所有配置最佳 `scale=0` | 部署式插值的八个运输参数均造成负收益 |

### 6.3 复残差和功率先验 O9–O12

| 尝试 | 结果 | 失败原因 |
|---|---:|---|
| O9 复残差表示 Oracle | `0.801341` | target-visible，仅证明表示容量，不能部署 |
| O10 候选支撑 Oracle | 最高 `0.822253` | 使用真实目标残差值，不能直接推理 |
| O11 候选复残差回归 | 最佳 `0.683729`、`scale=0` | 2000 样本无法泛化快速变化的复相位 |
| O12 WRF-GS 角功率先验 | `0.690381`，增益 `+0.000275` | PAS 略升，但 PDP/NMSE 恶化，未超过 O4.1 |

### 6.4 Diffusion O13–O15

| 尝试 | 结果 | 失败原因 |
|---|---:|---|
| 旧 Residual PCA Diffusion | 最佳残差 scale `0.1` | 完整 latent 加性残差破坏 PAS/PDP |
| O13 候选复系数 Diffusion | 验证始终 `scale=0` | 稀疏复残差与单位噪声尺度失衡；结构未忠实匹配参考实现 |
| O14 Angle-Delay 功率 Oracle | `0.809782` | target-visible，只证明真实幅度替换的上限 |
| O15 v1 2D 功率 Diffusion | 首轮 `0.177900` | zero-terminal-SNR 与 epsilon 反演导致采样崩坏 |
| O15 v2 v-prediction | 第10轮 `0.528059` | 从纯噪声生成完整功率图，训练—采样鸿沟过大 |
| O15 v3 coarse-init | 第1轮 `0.676704`，第5轮 `0.660737` | 初始接近基线但训练后退化，未超过 O4.1 |

### 6.5 O16/O17 最终结构审计

| 路线 | 结果 | 结论 |
|---|---:|---|
| Route A 稀疏功率 Oracle | `0.793479` | target-visible、`deployable=false`，不是比赛成果 |
| Route C 局部波前 | `0.690106`，margin `0` | 所有修正均回退 O4.1，零增益 |
| Route B Gaussian 单次反射 | `0.158806` | 真实地图低于打乱控制组，解析映射不可辨识 |

最终三路线虽然形式上选择 A 作为“表示方向”，但三者都没有形成可部署的新提交，因此全部从
活动工程中移除。

## 7. 给新对话的明确结论

1. 当前不可破坏的官方最佳是 O4.1：`0.552317`。
2. 不要恢复已删除的 O5–O17 或旧 diffusion 分支，它们已有明确负结果。
3. 不要把 target-visible Oracle 分数当作可部署分数。
4. O4.1 的有效点是“逐 Anchor 信道 + 正确 Anchor→Target 点云走廊 + 因果对照”，不是单纯
   增大网络或增加训练轮数。
5. 本地 5% coverage 验证与线上存在差距；任何后续尝试必须先胜过 O4.1 的 real/zero/shuffle
   门槛，再考虑生成提交。
6. 若服务器的 day3/day7/day10/day13/day14/day18 资产仍在，应立即备份；它们是复现
   `0.552317` 的关键，当前本地工程没有这些 checkpoint。
