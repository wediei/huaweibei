# 项目方案迭代记录

> 本文件是长期研究架构师、执行者和验证者共享的结构化项目状态。  
> 详细实验历史、复现命令与失败总表以 `总结.md` 为准；面向用户的简要过程记录以 `项目记录.md` 为准。

## 1. 项目身份与长期目标

- 项目根目录：`D:\all_prj\huaweibei_advanced`
- 任务：华为杯 Round1 无线信道预测。
- 长期目标：在保留现有有效工作的基础上持续探索可部署的新方案，提高官方比赛分数并进入 32 强。
- 当前阶段：首次轻量接管已获用户确认，完成持久化上下文初始化；尚未批准新的源码研究、实现或训练任务。

## 2. 项目边界与事实来源

| 类型 | 位置 | 定位 |
|---|---|---|
| 官方约束 | `task/赛题任务书.pdf` | 任务输入、输出与评分规则 |
| 官方数据 | `dataset/Round1_Map/` | 训练位置、训练信道、测试位置与官方点云 |
| 参考项目 | `reference/` | WRF-GS、WRF-GS+、XFreq-GS、diffusion/BTD 等候选机制 |
| 旧改造基线 | `test/` | 用户原有的 WRF-GS+ 适配版本，不作为当前最佳方案 |
| 当前实现 | `solution/` | 当前保留的 O4.1 方案及其运行依赖 |
| 官方成绩证据 | `results/8_0.552317/` | 得分 `0.552317` 对应的提交产物 |
| 完整项目总结 | `总结.md` | 当前方案、复现流程、成绩和失败路线 |
| 用户过程记录 | `项目记录.md` | 用户要求与修改结果的简要记录 |

## 3. 当前接受基线

### BASE-001：O4.1 Gaussian Anchor Transport

- 状态：`ACCEPT`
- 官方线上得分：`0.552317`
- 前置可部署基线：Power Refiner V1，得分 `0.547709`
- 当前证据：`总结.md` 与 `results/8_0.552317/`
- 方案要点：固定 Beam-Delay 压缩、冻结 coarse/refiner、K=4 逐 Anchor 信道运输、正确的 `Anchor→Target` Gaussian 走廊、real/zero/shuffle 因果对照。
- 项目含义：这是当前比较基准和回退基线，但不限制研究新的竞争方案。

## 4. 核心资产与风险

| 项目 | 当前状态 | 风险或待确认事项 |
|---|---|---|
| O4.1 源码与官方得分文件 | 本地存在 | 后续变更需保留可比较和可回退证据 |
| day3/day7/day10/day13/day14/day18 服务器资产 | 本地不完整，服务器状态未知 | 若已丢失，无法直接复现同一 `0.552317` checkpoint |
| 本地验证体系 | 已有 5% coverage 结果 | 与线上存在差距，不能只凭单一 coverage 结果晋级 |
| 数据契约 | 历史观察存在 `P_Train=2500` 与实际 2000 条的差异 | 进入研究阶段后需定向复核，不能只信配置值 |
| 比赛资源 | 未确认 | 32 强分数线、剩余提交次数、GPU 预算和服务器存储状态待补充 |

## 5. 已知研究边界

- O5–O17 与旧 residual diffusion 路线已有明确负结果，不应在没有新假设时直接恢复。
- target-visible Oracle 只表示上限或表示容量，不属于可部署比赛方案。
- O4.1 的有效证据来自正确地图与信道的绑定；单纯扩大网络或增加训练轮数不是已证实方向。
- 新研究应围绕具体信息缺口，按概览、接口、数据流和核心机制逐层读取所需源码，证据充分后停止。
- WRF-GS、WRF-GS+、XFreq-GS 和 diffusion/BTD 目前只登记为候选机制，不代表已选定研究路线。

## 6. 方案迭代表

| 方案 ID | 假设或方向 | 范围 | 状态 | 核心证据 | 决策 |
|---|---|---|---|---|---|
| BASE-001 | Gaussian 路径可指导逐 Anchor 低维信道运输 | O4.1 | 已完成 | 线上 `0.552317`；real-map 优于 zero/shuffle | `ACCEPT` |
| TASK-018 | 多个 Beam-Delay 分量独立运输可突破单组八参数上限 | target-visible Oracle | 已批准，待执行 | `L=1/2/3` 表示容量对比 | 待决策 |

后续方案使用唯一 ID 追加记录，不覆盖旧结论。每项只记录相对当前基线的差异、关键证据、资源消耗和决策；详细任务定义与执行结果分别放入 Spec 和执行报告。

## 7. 决策记录

### 2026-07-30：初始化长期方案记录

- 用户批准首次轻量接管形成的临时理解与提纲。
- 将 O4.1 登记为当前接受基线。
- 保留服务器资产、验证偏差、数据契约和比赛资源四类风险。
- 未批准源码研究、仓库索引、实现、训练、部署或分支操作。

### 2026-07-30：批准 TASK-018 多分量运输 Oracle

- 用户批准 `TASK-018-multipath-oracle`，GitHub 为 `https://github.com/wediei/huaweibei`。
- 服务器 day3/day7/day10/day13/day14/day18 关键资产已由用户确认存在。
- 当前远端 `main` 是早期 WRF-GS+ 基线；O4.1 将使用独立基线分支，不覆盖 `main`。
- 本任务只验证多分量运输的 target-visible 表示上限，不训练模型、不生成提交。

## 8. 执行约束

- SSH 环境使用 `mamba` 代替 `conda` 执行环境安装与创建。
- PyTorch 2.0.1、torchvision 0.15.2、torchaudio 2.0.2 使用 CUDA 11.8 wheel 单独安装；环境 YAML 不写入 torch 相关依赖，并明确说明此约束。
- 测试脚本使用 `python` 运行，不使用 `conda run`。
- 所有训练必须使用 tqdm 等方式展示进度，并保留过程日志。
- 正式执行任务需要 Git 仓库、用户批准的远端和已提交起点；数据、缓存和 checkpoint 默认不进入普通 Git。

## 9. 下一批准边界

下一阶段应由架构师提出一个有边界的创新研究工作定义，明确目标、理由、范围、交付物、排除项、资源、验收条件和停止条件，并获得用户明确批准后再开始。当前记录的建立不自动授权任何研究或实现活动。
